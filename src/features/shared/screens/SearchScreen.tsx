import { Component as PremiumScreen } from "@/components/ui";

export default function SearchScreen() {
  return (
    <PremiumScreen
      badge="Search"
      description="Search citizens, reports, tasks, and locations through a premium, low-noise input surface."
      primaryActionLabel="Search"
      secondaryActionLabel="Advanced filters"
      subtitle="Responsive and accessible across light and dark contexts."
      title="Search"
    />
  );
}
