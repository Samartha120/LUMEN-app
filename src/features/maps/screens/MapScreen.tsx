import { Component as PremiumScreen } from "@/components/ui";

export default function MapScreen() {
  return (
    <PremiumScreen
      badge="Live Map"
      description="Explore civic locations through a calm, premium map entry point with sharp spatial hierarchy."
      primaryActionLabel="Center map"
      secondaryActionLabel="Search"
      subtitle="Built for location-first work with smooth transitions and low clutter."
      title="Map"
    />
  );
}
