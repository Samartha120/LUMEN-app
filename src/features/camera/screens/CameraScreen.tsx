import { Component as PremiumScreen } from "@/components/ui";

export default function CameraScreen() {
  return (
    <PremiumScreen
      badge="Camera"
      description="Capture media through a premium camera shell with strong contrast and a calm control surface."
      primaryActionLabel="Take photo"
      secondaryActionLabel="Gallery"
      subtitle="A focused capture experience for reports and proofs."
      title="Camera"
    />
  );
}
