import { Component as PremiumScreen } from "@/components/ui";

export default function RegisterScreen() {
	return (
		<PremiumScreen
			badge="Create Account"
			description="Join the platform with a premium onboarding flow designed for trust, clarity, and completion."
			primaryActionLabel="Create account"
			secondaryActionLabel="Already registered"
			subtitle="A streamlined start for civic access across every role."
			title="Create your LUMEN account"
		/>
	);
}
