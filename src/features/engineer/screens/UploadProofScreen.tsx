import { Component as PremiumScreen } from "@/components/ui";

export default function UploadProofScreen() {
  return (
    <PremiumScreen
      badge="Proof"
      description="Attach evidence with a premium upload surface designed for confidence and precision."
      primaryActionLabel="Upload file"
      secondaryActionLabel="Add later"
      subtitle="Built to feel fast on the move and clear in every state."
      title="Upload proof"
    />
  );
}
